<%= form_for(@bank_account_type) do |f| %>
  <% if @bank_account_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank_account_type.errors.count, "error") %> prohibited this bank_account_type from being saved:</h2>

      <ul>
      <% @bank_account_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
